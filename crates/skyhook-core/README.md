# Skyhook Core

`skyhook-agent-core` is the reusable runtime behind the `skyhook-agent` command-line package.
Its Rust library crate is named `skyhook`.

See the [Skyhook repository](https://git.kryesh.tech/Apps/skyhook) for configuration and usage.
